### Prerequisites

- Rust + Cargo
- A `VENICE_API_KEY`
- `node` and `npm` only if you want Playwright browser automation

### Basic usage

```bash
export VENICE_API_KEY="your-key-here"
cargo run 
```
